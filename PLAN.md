# Jang dong ho — Portfolio Site 기획서

> BX 디자이너 개인 포트폴리오 사이트
> 최초 작성: 2026-08-26

---

## 1. 개요

| 항목 | 내용 |
|---|---|
| 목적 | 이직용 개인 포트폴리오 |
| 소유자 표기 | `Jang dong ho` |
| 프로젝트 수 | 11~14개 |
| 콘텐츠 출처 | 기존 비핸스 프로젝트를 이전 |
| 백엔드 | **없음** (정적 사이트) |
| 배포 | Vercel (GitHub `igujang/Jangportfolio` 연동, 푸시 시 자동 배포) |

### 핵심 컨셉
좌측에 작업 제목이 나열되고, **마우스를 올리면(호버) 우측 하단 썸네일이 해당 작업으로 바뀐다.**
클릭하면 비핸스 규격의 상세 페이지로 진입한다.

참고: [skiper-ui.com/v1/skiper24](https://skiper-ui.com/v1/skiper24) — 단, **배경색 전환과 무한 스크롤은 사용하지 않음**

---

## 2. 기술 스택

| 영역 | 선택 | 이유 |
|---|---|---|
| 프레임워크 | **Next.js (App Router) + TypeScript** | 프로젝트별 고유 URL 생성, Vercel 최적 연동, 정적 생성으로 빠름 |
| 스타일 | **Tailwind CSS** | 반응형 대응이 빠름 |
| 애니메이션 | **Framer Motion** | 호버 전환·페이지 전환 처리 |
| 폰트 | **영문 Neo-grotesque + Pretendard(국문)** | 참고 사이트 톤. 최종 폰트는 디자인 단계에서 확정 |
| 이미지 | Next.js `<Image>` | 자동 최적화·지연 로딩 |

---

## 3. 사이트 구조

```
/                     메인 — 프로젝트 리스트 + 썸네일 프리뷰
/works/[slug]         프로젝트 상세 (프로젝트당 1개, 고유 URL)
/about                이력 페이지  ※ 내용 미정, 자리만 확보
```

프로젝트마다 URL이 생기므로 **"이 작업만 보세요" 링크를 이력서에 첨부 가능.**

---

## 4. 메인 페이지

### 4-1. 레이아웃 (1920px 기준)

```
┌──────────────────────────────────────────────────┐
│                                                  │
│  Jang dong ho                     ← 최상단 고정   │
│                                     클릭 → /about │
│  01   LOCL Brand Identity                        │
│                                                  │
│  02   참이슬 Renewal                              │
│                                                  │
│  03   Project Name                               │
│                                                  │
│  ...                                             │
│                                    ┌───────────┐ │
│  14   Project Name                 │           │ │
│                                    │  썸네일    │ │
│                                    │  (영상)    │ │
│                                    └───────────┘ │
└──────────────────────────────────────────────────┘
   배경: 흰색 고정 (#FFFFFF)
```

- **좌측 리스트**: `번호 + 제목`만. 카테고리·연도 없음
- **최상단 `Jang dong ho`**: 고정 노출, 클릭 시 `/about` 이동, 호버 시 썸네일 영역에 본인 이미지 노출
### 썸네일 영역 규격

| 항목 | 값 |
|---|---|
| 비율 | **1:1 정사각형 (고정)** |
| 크기 (1920 기준) | 약 400 × 400px |
| 위치 | 우측 하단 고정 |
| 여백 | 우측·하단 각 48px |
| 모서리 | 라운드 처리 (약 16px) |
| 재생 | 자동재생 · 무음 · 무한 루프 |
| 반응형 | 화면 폭에 비례해 축소 (최소 180px) |

### 4-2. 인터랙션 (데스크탑)

| 동작 | 결과 |
|---|---|
| 항목에 **호버** | 해당 항목 활성화 + 썸네일 영역에 해당 작업 영상 재생 |
| 항목 **클릭** | 상세 페이지 진입 |
| 아무데도 호버 안 함 | 직전 선택 유지 (초기값 = 01번) |

**시각 처리**
- 비활성 항목: 연한 회색
- 활성 항목: 검정
- 전환은 부드럽게 (색상·썸네일 모두 페이드)

### 4-3. 인터랙션 (모바일 / 태블릿)

레이아웃은 **동일하게 유지**하고 선택 방식만 바꾼다.

```
┌─────────────────┐
│ Jang dong ho    │
│                 │
│  01  프로젝트A   │
│  02  프로젝트B   │
├─ 03  프로젝트C ──┤ ← 화면 중앙 '선택 라인'
│  04  프로젝트D   │   이 라인에 걸린 항목이 자동 활성화
│  05  프로젝트E   │
│          ┌────┐ │
│          │썸네일│ │
└──────────└────┘─┘
```

- **위아래로 밀면** 화면 중앙 라인을 지나는 항목이 자동 선택 → 썸네일 교체
- **탭하면 바로 진입** (2단계 탭 없음 — 미리보기는 스크롤이 담당하므로)
- 썸네일 크기는 화면에 맞춰 축소, 우측 하단 위치 유지

### 4-4. 반응형 기준점

| 구간 | 대응 |
|---|---|
| 1920px | 최적화 기준 |
| 1440~1919px | 비율 축소 |
| 768~1439px (태블릿) | 제목 크기 축소, 썸네일 축소 |
| ~767px (모바일) | 스크롤 기반 선택으로 전환 |

---

## 5. 상세 페이지 `/works/[slug]`

### 5-1. 구성 — **비핸스 규격**

```
┌──────────────────────────────────────┐
│ ←  (돌아가기)                         │
│                                      │
│      ┌────────────────────┐          │
│      │                    │          │
│      │   이미지 01         │  1400px  │
│      │                    │          │
│      ├────────────────────┤          │
│      │   이미지 02         │          │
│      ├────────────────────┤          │
│      │   영상 (임베드)      │          │
│      ├────────────────────┤          │
│      │   이미지 03         │          │
│      └────────────────────┘          │
│                                      │
│         다음 프로젝트 →               │
└──────────────────────────────────────┘
```

**상단 정보 블록 없음.** 제목·설명·연도 등은 이미지 안에 이미 포함되어 있으므로 **바로 작업물부터 시작**한다.

| 항목 | 규격 |
|---|---|
| 콘텐츠 폭 | 최대 **1400px**, 중앙 정렬 (비핸스 권장 폭과 동일) |
| 이미지 배치 | 폭에 꽉 차게, 세로로 연속 |
| 이미지 간격 | 기본 0 (붙여서) |
| 배경 | 흰색 |
| 모바일 | 화면 폭에 꽉 차게 |

### 5-2. 네비게이션
- 좌상단에 **돌아가기** — 메인으로 복귀
- 하단에 **다음 프로젝트** — 뒤로 갔다 다시 들어오는 수고 없이 연속 감상

### 5-3. 영상 — **비메오 임베드 (핵심 기능)**

최근 작업물은 비메오에 업로드해 임베드하는 방식으로 제작되어 있으므로, **이미지와 영상이 섞여서 세로로 나열되는 구조**를 기본으로 한다.

```
      ┌────────────────────┐
      │   이미지 01         │
      ├────────────────────┤
      │                    │
      │   비메오 영상 ▶      │  ← 이미지와 동일한 1400px 폭
      │                    │
      ├────────────────────┤
      │   이미지 02         │
      ├────────────────────┤
      │   비메오 영상 ▶      │
      └────────────────────┘
```

| 항목 | 규격 |
|---|---|
| 폭 | 이미지와 동일 (최대 1400px) |
| 비율 | 16:9 기본, 영상별 지정 가능 |
| 위치 | 이미지 사이 **어느 위치에나** 삽입 가능 |
| 개수 | 프로젝트당 제한 없음 |
| 저장 용량 | **0** (비메오가 호스팅·최적화 담당) |

### 영상 두 종류

비핸스에서 영상이 **컨트롤 없이 무한 반복**되는 것은 비메오의 `background` 모드다. 두 방식을 모두 지원한다.

| 구분 | 동작 | 용도 |
|---|---|---|
| **루프 영상** | 자동재생 · 무음 · **무한 반복** · 재생 버튼 등 컨트롤 전부 숨김 | 모션 그래픽, 로고 애니메이션, 무빙 이미지 — 이미지처럼 흘러가야 하는 것 |
| **일반 영상** | 클릭해서 재생 · 소리 있음 · 컨트롤 노출 | 캠페인 영상, 나레이션 있는 영상 |

루프 영상은 비메오 임베드 주소에 `background=1` 파라미터를 붙여 처리한다.
(자동재생 · 반복 · 무음 · UI 숨김이 한 번에 적용됨)

---

## 6. 이력 페이지 `/about`

- **레이아웃(자리)만 우선 구현, 내용은 추후 입력**
- 내용은 `content/about.md` 파일로 분리
- 수정 방법: **GitHub 웹사이트에서 해당 파일만 편집 → 저장 → Vercel이 1~2분 내 자동 반영**
  (PC·터미널 불필요, 폰에서도 가능. 사실상 무료 CMS 역할)

---

## 7. 데이터 · 폴더 구조

```
public/works/
├─ 01-locl/
│   ├─ thumb.mp4        ← 움직이는 썸네일 (5~8초 루프)
│   ├─ 01.jpg           ← 상세 페이지 콘텐츠
│   ├─ 02.jpg              파일명 순서대로 자동 나열
│   ├─ 03.loop.txt      ← 무한 루프 영상 (컨트롤 없음)
│   ├─ 04.jpg
│   └─ 05.vimeo.txt     ← 일반 영상 (클릭 재생, 소리 있음)
├─ 02-chamisul/
└─ ...

content/
├─ works.json           ← 번호·제목·slug
└─ about.md             ← 이력 내용
```

### 핵심 규칙 — 파일명 순서 = 화면 순서

폴더 안의 파일을 **이름순으로 읽어서 그대로 세로 나열**한다.

| 파일 | 화면에 나오는 것 |
|---|---|
| `01.jpg` / `.png` / `.webp` | 이미지 |
| `03.loop.txt` | **무한 루프 영상** — 자동재생, 무음, 컨트롤 없음 |
| `05.vimeo.txt` | **일반 영상** — 클릭 재생, 소리 있음 |
| `thumb.mp4` | 썸네일 (본문에는 안 나옴) |

`.txt` 파일 안에는 비메오 주소 한 줄만 적으면 된다:
```
https://vimeo.com/1234567890
```

→ **이미지든 영상이든 파일명 번호만 맞추면 원하는 순서로 배치된다.** JSON을 건드릴 필요가 없고, 순서를 바꾸려면 파일명 숫자만 바꾸면 된다.

### works.json 형태 (예시)

```json
[
  {
    "number": "01",
    "title": "LOCL Brand Identity",
    "slug": "locl",
    "folder": "01-locl"
  }
]
```

프로젝트 자체를 추가·삭제·순서변경할 때만 이 파일을 수정한다.

---

## 8. 작업 추가 워크플로우

```
1. public/works/ 아래에 폴더 생성
2. 썸네일 영상(thumb.mp4) 넣기
3. 이미지들을 01.jpg, 02.jpg... 순서대로 넣기
4. 영상 자리에는 03.vimeo.txt 파일을 만들고 비메오 주소 한 줄 입력
5. content/works.json 에 항목 한 줄 추가
6. git push  →  Vercel 자동 배포 (1~2분)
```

---

## 9. 에셋 규격

| 종류 | 규격 |
|---|---|
| 상세 이미지 | 폭 **1400px** 이상, JPG (또는 WebP) |
| 썸네일 영상 | **mp4 / webm**, 5~8초 루프, 무음, **10MB 이하** (권장 1~3MB) |
| 본문 영상 | **비메오 업로드 후 주소만 사용** — 저장소 용량 0 |

**GIF 대신 mp4/webm 사용** — 같은 화질에서 용량이 1/10~1/20로 줄어든다.

> ⚠️ `.gitignore`에서 `*.mp4`, `*.mov`, `*.zip` 제외 규칙을 수정해야 함
> (썸네일 mp4는 저장소에 포함되어야 하므로)

---

## 10. 확정 사항 요약

- [x] 백엔드·데이터베이스 **없음**
- [x] 배경 흰색 고정 (참고 사이트의 컬러 전환 미사용)
- [x] 무한 스크롤 미사용
- [x] 좌측 리스트: 번호 + 제목만
- [x] 상세 페이지: 상단 정보 블록 없이 바로 이미지
- [x] 상세 진입: 별도 페이지 (고유 URL)
- [x] 모바일: 레이아웃 유지, 스크롤 위치 기반 선택
- [x] 이름 표기: `Jang dong ho`
- [x] 본문에 **비메오 임베드 영상**이 이미지와 섞여 나열됨

## 11. 미확정 · 추후 결정

- [ ] **이력 페이지 내용** — 추후 전달 예정
- [ ] **폰트 확정** — 영문/국문 조합
- [ ] **프로젝트 목록** — 실제 14개 제목·순서·slug
- [ ] **리스트가 한 화면을 넘칠 경우 처리** — 항목 크기를 화면 높이에 맞춰 자동 축소할지, 스크롤을 허용할지
- [ ] **도메인** — 우선 Vercel 기본 주소 사용, 추후 연결
```
