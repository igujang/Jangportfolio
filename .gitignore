# ── OS ─────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Editor ─────────────────────────
.vscode/
.idea/
*.swp

# ── Node / Build ───────────────────
node_modules/
dist/
build/
.next/
out/
.cache/
*.log
.env
.env.local
.vercel

# ── 디자인 원본 (웹용으로 변환한 것만 커밋) ──
*.ai
*.psd
*.psb
*.sketch
*.fig
*.indd
*.aep
*.prproj
*.mov
*.zip

# 원본/작업 파일 보관용 폴더
/source/
/_raw/

# ── 예외: 사이트에 실제로 쓰이는 썸네일 영상은 포함 ──
!public/works/**/*.mp4
!public/works/**/*.webm

# ── 원본 자료 (로컬 보관, 변환본만 커밋) ──
/Project/
/ref/
*.ai
memo.txt
/Font/
/Jand dong ho profile/
